--- 
clone: 
  depth: 1
kind: pipeline
name: FiqriPipeline
steps: 
  - 
    commands: 
      - "curl https://raw.githubusercontent.com/fiqri19102002/build_script/droneci-test/kernel.sh | bash"
    environment: 
      TELEGRAM_TOKEN: 
        from_secret: BOT_API_KEY
    image: fadlyas/kernel_dockerfile:latest
    name: compile
    trigger: 
      branch: 
        - eas-sdm660-test
